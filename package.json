{
  "name": "sop-agent",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.23.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "spike:generate-fixtures": "pnpm --filter @sop-agent/extraction-spike generate-fixtures",
    "spike:extraction": "pnpm --filter @sop-agent/extraction-spike start",
    "dev:api": "pnpm --filter @sop-agent/api dev",
    "dev:web": "pnpm --filter @sop-agent/web dev",
    "dev": "pnpm -r --parallel --filter @sop-agent/api --filter @sop-agent/web dev",
    "smoke:api": "pnpm --filter @sop-agent/api smoke",
    "eval": "pnpm --filter @sop-agent/api eval",
    "eval:recheck": "pnpm --filter @sop-agent/api eval:recheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
