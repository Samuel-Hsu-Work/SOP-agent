# Copy this file to apps/web/.env.local. Next.js reads it from this folder, not from the repository root.
# The only value the browser app needs is where the API runs. Never put OPENAI_API_KEY here.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
