# TODO / Working Notes

Working state that isn't visible from the code alone — what's in progress, what's next. Update the **Status** section before ending a work session (or ask Claude to do it) so a new chat or a fresh IDE session can pick up context immediately from this file + `git log`.

**This file is a snapshot, not ground truth.** It may be stale if a session ended without updating it. Before acting on anything below, ground it against the repo: run `git log`/`git status`/`git diff` and spot-check that the files/features mentioned still look the way this file says they do. If it's drifted, fix the entry (or flag the drift) rather than trusting it blindly.

## Status (last updated: 2026-09-21)

- All six planned slices, the higher per-message limits, the approval-basis header and the consistency review are built and pushed (`main` = `origin/main` = `68989b8`).
- **Slice 7 (deployment) is done locally and not committed, scoped down on purpose at the user's request:** no rate limiting, no daily counter and no parser worker thread. What it holds: `render.yaml` (the API on Render), a "Deploy" section in the README (spending limit on the OpenAI project first, then Render, then Vercel with Root Directory `apps/web`, then `WEB_ORIGIN`), `tsx` moved to the API's runtime dependencies (the start command needs it in a production install), `WEB_ORIGIN` reduced to its origin (a trailing slash would have silently broken CORS), and a trailing slash stripped from `NEXT_PUBLIC_API_BASE_URL`. Checked in a fresh clone the way Render builds it: a production-only install starts the API on `0.0.0.0` and the given port, `/health` answers, CORS allows only the configured origin, and `next build` succeeds with the API address set. **Not done and needs the user:** the actual deployment (accounts and keys), and setting the spending limit on the OpenAI project.
- The only protection against strangers spending credit is on the OpenAI side, and the README says so: a separate project, prepaid credit with automatic recharge off (the balance is the hard limit), the address not posted publicly, and the deployment taken down after the review. The existing bounds still apply (2 MiB upload, 60 pages, 100,000 characters, two extractions at a time, 12,000 characters a message). Codex noted (P1) that a project budget may only alert and not block; the README no longer claims it blocks. If the address will be shared widely, the smallest real guard is an in-memory per-IP limit with `@fastify/rate-limit`, or a shared access code.
- Live evidence is current: `pnpm smoke:api` passes on both models, and the last full `EVAL_MODELS=all pnpm eval` had every safety assertion pass on every trial (the remaining behavior misses are the known luna slips). `docs/` is git-ignored and not backed up.

## In progress

- Slice 7 awaits the Codex review of its diff, then a commit and a push on the user's go-ahead.

## Next up / backlog

- **Deliberately not done (user decision, keep it simple):** rate limiting for `/chat`, `/documents/extract` and `/sops/pdf` (the smallest version is `@fastify/rate-limit` per IP, in memory), a daily counter, a shared access code, and a hard parser timeout in a worker thread. Revisit if the deployed address is shared widely.
- **Known limit, accepted (Codex round 3, P1):** two uploads with the same sanitized file name count as one source, so a revised same-named file cannot conflict with the earlier one (a handbook's own $500/$5,000 ladder must not flag itself). A fix needs an upload id in the citation (schema v6). Decided 2026-09-20: not fixed in v1; revisit if a demo needs a revised file.
- **Conflict heuristic tuning:** `CONFLICT_TOPIC_OVERLAP` (0.5) also applies to statements with equal figures, on purpose (over-flag rather than miss). If a demo shows too many conflicts for restatements, raise the overlap to 0.6 or require a differing word when the figures are equal.
- **Prompt and behavior observations, not yet acted on:** the agent sometimes adds an effective date the user did not state; `gpt-5.6-luna` once miscounted in a confirmation ("6 claims across 6 fields" while listing 5), and putting the active claim count in the state item would make counts reliable. Re-run the eval after any prompt change.
- **Possible polish:** one click per suggestion or per extracted claim may be too many in a demo (a per-field tick is a `checkFinalization` change); the approval panel lists suggestions but does not jump to them; a second upload of the same file costs one model call before the duplicate check drops its claims; upload progress is a busy label, not a bar; a PDF font that draws non-Latin text is the recorded upgrade path (decision 36); the OpenAI SDK's error classes leak into `modelFallback.ts`, `logging.ts` and `routes/chat.ts` (provider-neutral error types would fix it).
- **Loose end:** `next dev` generated `apps/web/AGENTS.md` and `apps/web/CLAUDE.md`, which are committed. Remove them if they are unwanted.
- **Back up `docs/`:** either remove `docs/` from `.gitignore` (the repo is private) or copy it elsewhere.

## Recently done

- 2026-09-20 — **Slice 5, document ingestion (built, verified offline and live, reviewed by Codex twice; not yet committed).** Session schema v5 (`policy_document` source with a citation inside the reference, `conflictsWithClaimId`, `conflict_detected` and `conflict_resolved` history); `ingestExtracted` and `resolveConflict` in `applyClaim`, `detectConflicts.ts`, `documentWire.ts`; in `apps/api`: bytes-based bounded `parseDocument` (PDF columns and tables from coordinates, DOCX through an HTML parser, Markdown fences and setext headings, magic-byte checks, unzip inflation cap), hardened `verifyQuote` (hyphenated breaks, length limits, de-duplication), `extractClaimDrafts` behind `runWithModelFallback` with a JSON user-role input and no file name, `POST /documents/extract` (multipart, no disk, two at a time, counts-only log line), the `resolve_conflict` tool and prompt rules; in the browser: an upload panel, citations, side-by-side conflicts, all-or-nothing writes with a size guard; three eval scenarios and a repaired `never-confirms`; `pnpm measure:extraction` and a harder fixture set; the spike deleted. Planned through `/scope` with independent designs from Codex and an Opus planner; the user chose that the API returns drafts and the browser writes them, and a generous heuristic that also compares two documents. Live: extraction 97.8% verified, eval 34/34. Codex round 1 (truncation reported, agenda picks one real pair, source labels, neutral upload report) and round 2 (equal-figures conflicts, re-pairing after a resolution, rejected rules stay rejected, disconnect listener on the response) fixed. Decisions 42-54 in `docs/implementation-plan.md`.
- 2026-09-20 — **Slice 4, PDF export (pushed).** `POST /sops/pdf` with a recomputed four-part gate and one refusal code; `renderSopPdf` (pdfkit, Helvetica, A4, in memory); `<U+XXXX>` markers for characters the font cannot draw; session schema v4 with `downloadedAt`; a download button, the leave-page warning, the Acknowledged chip. Decisions 34-41.
- 2026-09-20 — **Slice 3, review and approval (pushed).** Session schema v3, confirm and reject, the approval step, the SOP preview, a code guard on confirmed claims, `happy-dom` component tests. Decisions 25-33.
- 2026-09-20 — **Slice 2, the full interview (pushed).** Session schema v2, one claim per procedure step, `applyClaim` with history for every change, the interview agenda, a static prompt plus a state item, a 14-scenario eval harness. Decisions 17-24.
- 2026-09-19 — **Slice 1, walking skeleton (pushed).** Three packages, streaming `POST /chat`, agent tool loop with whole-turn fallback, chat and readiness panels over `sessionStorage`.
- 2026-09-19 — Settled the whole design in `docs/` and Phase 0: workspace, sample documents, parsers, quote verification, extraction spike (43 of 43 claims verified on five clean files), switch to OpenAI (`gpt-5.6-sol`, fallback `gpt-5.6-luna`).

---
*Format note: keep entries short — this is a pointer to context, not a full changelog (that's what `git log` is for).*
