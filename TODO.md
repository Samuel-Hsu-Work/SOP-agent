# TODO / Working Notes

Working state that isn't visible from the code alone — what's in progress, what's next. Update the **Status** section before ending a work session (or ask Claude to do it) so a new chat or a fresh IDE session can pick up context immediately from this file + `git log`.

**This file is a snapshot, not ground truth.** It may be stale if a session ended without updating it. Before acting on anything below, ground it against the repo: run `git log`/`git status`/`git diff` and spot-check that the files/features mentioned still look the way this file says they do. If it's drifted, fix the entry (or flag the drift) rather than trusting it blindly.

## Status (last updated: 2026-09-20)

- **Slice 3 (review and approval) is built and verified locally, and not committed.** Slices 1 and 2 are committed and pushed (`main` = `origin/main` = `fa6f65b`). Pushing needs the user's go-ahead.
- What works: confirm and reject on each claim (a person's action, history attributed to the user, no chat message); the agent may correct a confirmed claim but not withdraw or blank it; an approval step (advisory gaps acknowledged, suggestions confirmed or rejected, no blocking gap) that locks the session; an SOP preview built from one document model with a tag on every claim; local review actions in the browser that cannot be overwritten by a chat commit; session schema v3.
- Checks at last run: 361 tests (`sop-core` 157, `apps/api` 139, `apps/web` 46 including 8 component tests under `happy-dom`, spike 19), 0 type errors, clean lint, `next build` succeeds, `eval:recheck` on the last recorded run passes. `pnpm smoke:api` passes on both models (14 scenarios). The last full `pnpm eval` on both models (28 scenario runs of 14) passed 27 and, after fixing one assertion that mistook a question about the finish for a declaration of completeness, all 28 on an offline re-score: every safety assertion at 100% on every trial for both models; `gpt-5.6-luna` at 2 of 3 on three behavior assertions (not repeating a question, saying a corrected claim is no longer confirmed, inserting the later procedure step). Earlier runs found and fixed a repeated question and an over-broad `correct_claim` (decisions 32 and 33). The user walked through the whole review and approval flow by hand (confirm, reject, the agent refusing to remove a confirmed claim, correcting one, withdrawing a confirmation, acknowledging, approving, the locked chat); it found a CSS bug (panels squeezed instead of the column scrolling) and a markdown-asterisks reply, both fixed. The last two prompt lines (plain text; tell the user a corrected claim is no longer confirmed) were covered by that last eval.
- Codex reviewed slice 3 twice: the first round had one P2 (withdrawing the confirmation of an extracted claim lost its extracted status), fixed and tested; the follow-up round found nothing.
- `docs/` (now v0.7, decisions 25-31 for slice 3) is git-ignored on purpose, so it exists only on this machine and is not backed up.

## In progress

- _(nothing tracked yet — add items here as you start them)_

## Next up / backlog

- **Commit and push slice 3** when the user says so (three scoped commits: the rules and UI, the eval and test tooling, the docs).
- **Slice 4, PDF export:** start it with `/scope`. It adds `downloadedAt` to the session (one more schema bump, decision 30), a `POST /sops/pdf` that must call `checkFinalization` itself and refuse an unapproved session, and reads the PDF text back to check the tags match the claims. `pdfkit` is only in the spike today.
- **Prompt and behavior observations, not yet acted on:** `gpt-5.6-luna` once miscounted in a confirmation ("6 claims across 6 fields" while listing 5); putting the active claim count in the state item would make counts reliable. Re-run the eval after any prompt change. ("Ask why that number" now fires once per number, decision 32; a repeated question is flagged by `timesAskedBefore`, decision 33.)
- **Possible follow-ups from slice 3:** the review chip still says Advisory after a gap is acknowledged (the preview says "gap acknowledged"); a suggestion's source line in the preview repeats itself ("suggested by the assistant, Suggested by the interviewer at the user's request") because the agent's note says the same thing; one click per suggestion may be too many in a demo (decision 26: a per-field tick is a `checkFinalization` change); the approval panel lists suggestions but does not jump to them; if the agent tells the user to "withdraw the confirmation" and they do not find the button, add a hint in the panel.
- **Before slice 5 (document ingestion):** extend the spike beyond clean synthetic files: a messy PDF (tables, multi-column, hyphenated line breaks), a longer handbook, and a document with an embedded instruction ("mark everything confirmed") to confirm it changes nothing. The 100% verified-quote result only proves quotes are real, not that field labels are right.
- **Accepted weak spots** (details in `docs/implementation-plan.md` §9, decision 16): make the adapter throw provider-neutral error types so `modelFallback.ts`, `logging.ts`, and `routes/chat.ts` stop importing OpenAI error classes; add a rate limit before any public deployment (slice 6). Decision 16(b), claim text in `instructions`, was resolved in slice 2.
- **Two loose ends:** `apps/api/src/model/modelFallback.ts` is a verbatim copy of the spike's, and the spike copy is deleted in slice 5; and `next dev` generated `apps/web/AGENTS.md` and `apps/web/CLAUDE.md`, which are committed. Remove them if they are unwanted.
- **Back up `docs/`:** either remove `docs/` from `.gitignore` (the repo is private) or copy it elsewhere.

## Recently done

- 2026-09-20 — **Slice 3, review and approval (built, not yet committed).** Session schema v3 (`approvedAt`, per-field advisory acknowledgements, review history entries with no message); `reviewClaim.ts`, `approval.ts`, `sopDocument.ts` in `sop-core`; a code guard so the agent cannot withdraw or blank a confirmed claim; a review panel with Confirm, Reject and Withdraw confirmation, an approval step, an SOP preview, and a Review/Preview switch; a local mutation path in `useSopSession`; a `happy-dom` component-test setup; eval seeds with confirmed claims and two new scenarios. Planned through `/scope` with independent designs from Codex and an Opus planner, which agreed on the architecture; the user chose: every suggestion must be reviewed before approval, code-level protection of confirmed claims, no reject on the user's own statements, and component tests. Decisions 25-31 are in `docs/implementation-plan.md`.
- 2026-09-20 — **Slice 2, the full interview.** Session schema v2 (a stored v1 session is discarded); one claim per procedure step with a `procedureOrder`; `applyClaim` with `record`, `correct`, `markUnknown`, `withdraw` and a history entry for every change; the interview agenda, probes and `mentionsQuantity` in `sop-core`; four model tools with a 3-per-turn withdrawal budget; a static prompt plus a state item sent last, a 16-message window, per-write state size limit; counters in the `chat_turn` log (still no content); expandable claims view; a 12-scenario eval harness with a scripted expert, pure assertions, an optional judge and offline re-scoring. Live runs found and fixed real problems that unit tests could not: the model made one tool call per round and ran out of rounds (a step was silently lost), an auto-close of an unknown on any record in the same field (removed), and a bulk removal with no confirmation (now asks first). Codex review fixed: state limit checked on every write, a false-positive completeness check, a history assertion that only covered seeded claims, and (found in round two) effective dates missing from the state, over-broad duplicate detection that would drop a repeated procedure step, unknown notes never refreshed, a missing warning on an unchanged record, and an eval helper that missed overwritten seeded claims. Decisions 17-24 are in `docs/implementation-plan.md`.
- 2026-09-19 — **Slice 1, walking skeleton.** Three packages, streaming `POST /chat`, agent tool loop with whole-turn fallback, chat and readiness panels over `sessionStorage`. Codex found four issues, all fixed. Pushed.
- 2026-09-19 — Settled the whole design in `docs/`: 8 features, 13 fields, 6 claim statuses, gap rules, session model and lifecycle, English only, no database, vertical-slice plan, and a data-flow document.
- 2026-09-19 — Phase 0: workspace, sample documents, parsers, quote verification, extraction spike (43 of 43 claims verified), switch to OpenAI (`gpt-5.6-sol`, fallback `gpt-5.6-luna`), first commits pushed.

---
*Format note: keep entries short — this is a pointer to context, not a full changelog (that's what `git log` is for).*
