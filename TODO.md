# TODO / Working Notes

Working state that isn't visible from the code alone — what's in progress, what's next. Update the **Status** section before ending a work session (or ask Claude to do it) so a new chat or a fresh IDE session can pick up context immediately from this file + `git log`.

**This file is a snapshot, not ground truth.** It may be stale if a session ended without updating it. Before acting on anything below, ground it against the repo: run `git log`/`git status`/`git diff` and spot-check that the files/features mentioned still look the way this file says they do. If it's drifted, fix the entry (or flag the drift) rather than trusting it blindly.

## Status (last updated: 2026-09-20)

- **Slice 4 (PDF export) is built, verified, and walked through by hand, and is not committed.** Slices 1 to 3 are committed and pushed (`main` = `origin/main` = `9a7eac9`). Committing needs the user's go-ahead (suggested: three scoped commits, the rules and API, the web app, the dependencies and test tooling), and pushing needs it again.
- What works: an approved SOP downloads as a PDF. `POST /sops/pdf` recomputes the export gate itself (`canExportApprovedSop`: approved, no blocking gap, every advisory gap acknowledged, no unreviewed suggestion) and answers 409 `sop_not_approved` otherwise; the PDF is rendered in memory from the document model (document-control block, legend, 13 sections, a tag on every claim, an "open items, not instructions" block, `Page X of Y`); characters the font cannot draw print as `<U+XXXX>` markers with a notice and a logged count; session schema v4 adds `downloadedAt`, set by the browser only after the download starts and kept on later downloads; a leave-page warning while work would be lost (New chat stays one click, no confirmation); the review chip says Acknowledged; a suggestion's source line no longer repeats itself.
- Checks at last run: 291 tests (`sop-core` 173, `apps/api` 172, `apps/web` 72, spike 19), 0 type errors, clean lint, `next build` succeeds, `pnpm smoke:api` passes 14 of 14 on both models, offline `eval:recheck` passes. `pnpm eval` was not re-run: no prompt or agent code changed. The user walked the whole flow by hand (interview, review, acknowledge, approve, leave warning before and after download, PDF opened in a viewer with the layout, the tags and the code-point markers checked, repeat download keeping the first time, New chat).
- Codex reviewed slice 4 once and found nothing. One small change followed it (the blob URL is revoked one task after the click, not at once); tests re-ran, and no second Codex round was run for it.
- `docs/` (now v0.8, decisions 34-41 for slice 4) is git-ignored on purpose, so it exists only on this machine and is not backed up.

## In progress

- _(nothing tracked yet — add items here as you start them)_

## Next up / backlog

- **Commit slice 4** when the user says so, then `/ship` cleanup of `.claude/plans/` (the brief and the two drafts are still there; the approved plan was resolved into decisions 34-41 and deleted).
- **Slice 5, document ingestion:** start it with `/scope`. It promotes the spike's parsers, quote verification and `modelFallback.ts` into `apps/api` and deletes `spikes/extraction`. `pdfjs-dist` is only a dev dependency of `apps/api` today (PDF read-back in tests); the parser will need it as a real dependency. The body limit (1 MiB) will have to rise with the upload limit.
- **Slice 5 also adds the conflict UI.** Confirm and reject already handle `extracted` claims (a withdrawn confirmation goes back to `extracted`, tested with hand-built claims), so slice 5 mainly needs the ingestion path and the conflict resolution in chat.
- **Before slice 5:** extend the spike beyond clean synthetic files: a messy PDF (tables, multi-column, hyphenated line breaks), a longer handbook, and a document with an embedded instruction ("mark everything confirmed") to confirm it changes nothing. The 100% verified-quote result only proves quotes are real, not that field labels are right.
- **Prompt and behavior observations, not yet acted on:** `gpt-5.6-luna` once miscounted in a confirmation ("6 claims across 6 fields" while listing 5); putting the active claim count in the state item would make counts reliable. Re-run the eval after any prompt change. ("Ask why that number" fires once per number, decision 32; a repeated question is flagged by `timesAskedBefore`, decision 33.)
- **Possible polish:** one click per suggestion may be too many in a demo (decision 26: a per-field tick is a `checkFinalization` change); the approval panel lists suggestions but does not jump to them; if the agent tells the user to "withdraw the confirmation" and they do not find the button, add a hint in the panel; the PDF leaves white space at the foot of a page when a section heading would otherwise sit alone there (kept on purpose); a PDF font that draws non-Latin text is the recorded upgrade path (decision 36).
- **Accepted weak spots** (details in `docs/implementation-plan.md` §9, decisions 16, 34 and 41): `POST /sops/pdf` is unauthenticated and unlimited, so anyone can make a well-formed "approved" session into a PDF (it needs no model call, so it costs no model money); add a rate limit before any public deployment (slice 6). Make the adapter throw provider-neutral error types so `modelFallback.ts`, `logging.ts`, and `routes/chat.ts` stop importing OpenAI error classes.
- **Two loose ends:** `apps/api/src/model/modelFallback.ts` is a verbatim copy of the spike's, and the spike copy is deleted in slice 5; and `next dev` generated `apps/web/AGENTS.md` and `apps/web/CLAUDE.md`, which are committed. Remove them if they are unwanted.
- **Back up `docs/`:** either remove `docs/` from `.gitignore` (the repo is private) or copy it elsewhere.

## Recently done

- 2026-09-20 — **Slice 4, PDF export (built and walked through, not yet committed).** `POST /sops/pdf` with a recomputed four-part gate and one refusal code; `renderSopPdf` (pdfkit, Helvetica, A4, in memory, deterministic bytes) taking a `SopDocument`; `toPrintableText` turning unencodable characters into `<U+XXXX>` markers; session schema v4 with `downloadedAt` and `markSopDownloaded` (the one write an approved session allows); shared `httpErrorSchema` and `sopPdfFileName`; a download button, the leave-page warning, and an in-page "not downloaded yet" notice; `sourceLine` and `gapLabel` moved into the document model; the Acknowledged chip. Planned through `/scope` with independent designs from Codex and an Opus planner, which agreed on the architecture; the user chose `<U+XXXX>` markers for characters the font cannot draw. Decisions 34-41 are in `docs/implementation-plan.md`.
- 2026-09-20 — **Slice 3, review and approval (pushed).** Session schema v3, confirm and reject, the approval step, the SOP preview, a code guard on confirmed claims, a local mutation path in the browser, `happy-dom` component tests, and eval seeds with confirmed claims. Codex found one P2 in round one (withdrawing the confirmation of an extracted claim lost its extracted status; fixed and tested), and nothing in round two. Decisions 25-33.
- 2026-09-20 — **Slice 2, the full interview (pushed).** Session schema v2, one claim per procedure step, `applyClaim` with `record`, `correct`, `markUnknown`, `withdraw` and history for every change, the interview agenda, a static prompt plus a state item, a 14-scenario eval harness. Live runs and Codex found and fixed real problems (a lost step, an auto-closed unknown, a bulk removal with no confirmation). Decisions 17-24.
- 2026-09-19 — **Slice 1, walking skeleton (pushed).** Three packages, streaming `POST /chat`, agent tool loop with whole-turn fallback, chat and readiness panels over `sessionStorage`.
- 2026-09-19 — Settled the whole design in `docs/` (8 features, 13 fields, 6 claim statuses, gap rules, session model, English only, no database, vertical-slice plan, data-flow document), and Phase 0: workspace, sample documents, parsers, quote verification, extraction spike (43 of 43 claims verified), switch to OpenAI (`gpt-5.6-sol`, fallback `gpt-5.6-luna`).

---
*Format note: keep entries short — this is a pointer to context, not a full changelog (that's what `git log` is for).*
